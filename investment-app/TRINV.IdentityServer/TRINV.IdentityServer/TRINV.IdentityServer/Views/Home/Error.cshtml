@model TRINV.IdentityServer.Models.Errors.ErrorViewModel

<div class="error-page">
    <div class="lead">
        <h1>Error</h1>
    </div>

    <div class="row">
        <div class="col-sm-6">
            <div class="alert alert-danger">
                Sorry, there was an error

                @if (Model.Error != null)
                {
                    <strong>
                        <em>
                            : @Model.Error.Error
                        </em>
                    </strong>

                    if (Model.Error.ErrorDescription != null)
                    {
                        <div>@Model.Error.ErrorDescription</div>
                    }
                }
            </div>

            @if (Model?.Error?.RequestId != null)
            {
                <div class="request-id">Request Id: @Model.Error.RequestId</div>
            }
        </div>
    </div>
</div>
